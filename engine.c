#include <stdio.h>

extern unsigned char width, height ; 


extern void generateArea ( )
{
    extern char area [ width ];
    for ( unsigned char i = width ; i > 0 ; i-- )
    {
        area[i] = [height]
    }
} 

extern void printArea ( ) 
{
    for ( unsigned char x = width ; x > 0 ; x-- )
    {
        for ( unsigned char y = height ; y > 0 ; y-- )
        {
            
        }
    }
    return;
}